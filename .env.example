# ctrlscan environment variables
# Copy to .env and fill in your values (never commit .env to git)

# OpenAI API key for AI triage and fix generation
OPENAI_API_KEY=sk-...

# GitHub Personal Access Token
# Create a classic token at https://github.com/settings/tokens/new
# Grant read API access; add write access if you want ctrlscan to open PRs
GITHUB_TOKEN=ghp_...

# GitLab Personal Access Token (optional)
GITLAB_TOKEN=glpat-...

# Azure DevOps PAT (optional)
AZURE_DEVOPS_TOKEN=...
AZURE_DEVOPS_ORG=...

# Database (defaults to SQLite at ~/.ctrlscan/ctrlscan.db)
# CTRLSCAN_DATABASE_DRIVER=mysql
# CTRLSCAN_DATABASE_DSN=user:password@tcp(localhost:3306)/ctrlscan?parseTime=true

# Agent mode: triage | semi | auto
# CTRLSCAN_AGENT_MODE=triage

# Path to scanner binaries (defaults to ~/.ctrlscan/bin)
# CTRLSCAN_TOOLS_BIN_DIR=/usr/local/bin
